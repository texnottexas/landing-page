# Secrets & credentials (generic patterns only — specific filenames in .git/info/exclude)
.env
.env.*
*.key
*.pem
*.p12
*.pfx

# Dependencies
node_modules/

# Logs
*.log

# Local tooling
.plans/
.playwright-cli/

# Game data (personal, not for public repo)
data/enigma-beasts.json
data/titan-rune-reference.json
data/all-hero-gear.json
data/heroes.json
data/gear-hero-*.json
data/neutral-city-treasury.json

# UD Treasury distribution (private)
data/ud-round*-players.json
data/ud-round*-distribution-plan.json
data/ud-distribution-history.json
data/treasury-items.json
scripts/ud-distribute.js
scripts/ud-publish.js

# Local-only pages (kept on disk, not deployed)
pages/ducks.html
pages/vegeta.html
pages/battle-plan.html
pages/great-offers-preview.html

# Preview GIFs (local only)
*.gif

# Design docs (local only)
2026-03-21-guest-identity-display-prefs-design.md
